#include <bits/stdc++.h>
using namespace std;

void power(double a, double n){
    double ans = pow(a, n);
    cout << ans;
}


int main(){
    double a, n;
    cin >> a >> n;
    power(a, n);
    return 0;
}